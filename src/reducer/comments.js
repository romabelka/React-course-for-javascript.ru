//import {  } from '../actions/constants'
import {comments} from '../fixtures'

export default ( state = {entities: comments, isLoading: false}, action) => {
    const { type, data } = action

    switch (type) {

    }

    return state
}